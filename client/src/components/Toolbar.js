import React from 'react';

const Toolbar = () => {
  return (
    <div className="toolbar">
      {/* <button>New</button>
      <button>Save</button>
      <button>Load</button> */}
    </div>
  );
};

export default Toolbar;
